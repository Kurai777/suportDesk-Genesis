# Segredos
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Ferramentas
.pytest_cache/
.ruff_cache/

# Base de documentação coletada da TOTVS (~8,3k arquivos / 42 MB).
# Conteúdo de TERCEIROS e derivado — não versionar. É reprodutível:
#   python -m scripts.coletar_central --temas <tema>   (ver COLETA.md)
# e ingerido no pgvector por `python -m scripts.ingest_docs`.
docs_totvs/

# Snapshots do banco (pg_dump) — ~37 MB, contêm a base inteira. Ver INFRA.md.
*.dump

# Logs locais
*.log

# IDE / SO
.vscode/
.idea/
.DS_Store
