# Dependências do serviço — versões fixadas (baseline conhecido).
# Somente as bibliotecas dos "Padrões de Engenharia" do CLAUDE.md.
# Qualquer outra dependência exige aprovação antes de entrar aqui.

# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Configuração e validação ---
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Clientes HTTP e retry ---
httpx==0.28.1
tenacity==9.0.0

# --- Claude e embeddings ---
anthropic==0.42.0
voyageai==0.3.2

# --- Banco vetorial (Postgres + pgvector) ---
psycopg[binary]==3.2.3
pgvector==0.3.6

# --- Busca web como último recurso (ADR-015; aprovada pelo usuário) ---
# ddgs: metabuscador (sucessor do duckduckgo-search). trafilatura: extrai o texto
# principal do HTML das páginas oficiais TOTVS.
ddgs==9.14.4
trafilatura==2.1.0

# --- Coletor da Central de Atendimento (ADR-017) ---
# lxml: parse/limpeza do HTML dos artigos Zendesk (já vem via trafilatura; fixado por
# ser importado diretamente em scripts/coletar_central.py).
lxml==6.1.1

# --- Login do Portal do Cliente TOTVS (ADR-026) ---
# playwright: dirige o browser no PortalLoginProvider (login 2FA). Import TARDIO em
# app/portal_login.py — só carrega quando o login é acionado. Deploy exige também o
# binário do browser: `python -m playwright install chromium`.
playwright==1.61.0
